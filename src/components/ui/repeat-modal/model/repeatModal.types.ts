import { IRepeatPhrase } from '@/app/(pages)/repeat-phrases/model/types/repeat-phrases.types';
import { IRepeatWord } from '@/app/types/repeat-words';

export type RepeatModalDataTypes = IRepeatPhrase | IRepeatWord;
